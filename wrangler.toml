# Cloudflare Pages Configuration
# This project is configured for Cloudflare Pages, not Workers

name = "vueplearance"
compatibility_date = "2025-11-14"

# Pages configuration
[site]
bucket = "dist"

# Build configuration
[build]
command = "npm run build"

# Disable Workers features
main = ""

# Asset configuration
[assets]
directory = "dist"